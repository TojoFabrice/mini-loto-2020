import { StyleSheet } from 'react-native';

export const MenuStyle = StyleSheet.create({
  container: {
    marginBottom: 30
  },
  image: {
    width: 40,
    height: 40,
    marginRight: 40
  },
});
